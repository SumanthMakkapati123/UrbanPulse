# Reference data

`route_schedule.csv` is a small schema-compatible fallback used for local development because the eLearn-provided CSV was not present in the supplied workspace. Replace its rows with the official file before recording the final demonstration; keep the header unchanged.

`zone_profile.csv` is a documented simulation fixture for the fictitious MetroConnect city. Its values must be described as simulated, not official government statistics.
